# Long-lived server secret. Never expose this as NEXT_PUBLIC_*.
OPENAI_API_KEY=

# Fastify demo server.
PORT=3001
WEB_ORIGIN=http://localhost:3000

# Local fixture repository selected for new demo tasks.
DEMO_REPO_ID=demo-repo
